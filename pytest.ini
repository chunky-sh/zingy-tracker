[pytest]
# The project is run from its checkout rather than installed, so the package
# lives at the repo root. `python -m pytest` happens to put that on sys.path;
# the bare `pytest` console script does not, which passed locally and failed in
# CI. Declaring it here makes both invocations work the same way.
pythonpath = .
testpaths = tests
